#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cf-node-debug/bin/cf-node-debug.js" "$@"
  ret=$?
else 
  node  "$basedir/../cf-node-debug/bin/cf-node-debug.js" "$@"
  ret=$?
fi
exit $ret
